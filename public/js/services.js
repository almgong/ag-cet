/**
 * JS file for the Scope of Services page.
 * Depends on JQ. Currently NOT used because
 * this page is not very Javascript intensive.
 * JS therefore found inline
**/

$(function() {
	$('#tab-container').easytabs();
});